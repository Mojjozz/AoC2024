#include <filesystem>
#include <fstream>
#include <iostream>
#include <vector>
#include <string>


std::string parse_input(std::ifstream &s);

int findWord(int r, int c, std::vector<std::string> input);
