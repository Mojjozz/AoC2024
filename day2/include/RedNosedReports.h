#include <iostream>
#include <vector>
#include <fstream>
#include <string>
#include <sstream>
#include <filesystem>

int processReport(std::vector<int>& level);
int processReportDampener(std::vector<int>& level);